import { BRAND, SHAPE, SIZE, STATE } from "../constants";

type TBrand = `${BRAND}`;

type TState = `${STATE}`;

type TShape = `${SHAPE}`;

type TSize = `${SIZE}`;

export type { TBrand, TShape, TSize, TState };
