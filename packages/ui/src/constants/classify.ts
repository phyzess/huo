enum BRAND {
  PRIMARY = 'primary',
  SECONDARY = 'secondary',
  ACCENT = 'accent',
  GHOST = 'ghost',
  LINK = 'link',
}

enum STATE {
  INFO = 'info',
  SUCCESS = 'success',
  WARNING = 'warning',
  ERROR = 'error',
}

enum SHAPE {
  SQUARE = 'square',
  CIRCLE = 'circle',
}

enum SIZE {
  LG = 'lg',
  SM = 'sm',
  XS = 'xs',
}

export { BRAND, STATE, SHAPE, SIZE };
